# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Runtime event log — regenerated by the app (bus appends here + replay
# reads/re-emits it). data/demo_events.jsonl (handcrafted) and
# data/ontology.yaml (baseline) are the actual source-controlled fixtures.
backend/data/events.jsonl
.env
backend/data/foundry.duckdb
backend/data/events.jsonl
.amr/
backend/data/uploads/
